apiVersion: apps/v1
kind: Deployment
metadata:
  name: DEPLOYMENT_NAME
  namespace: NAMESPACE
  labels:
    app: PROJECT
spec:
  selector:
    matchLabels:
      project: PROJECT
  template:
    metadata:
      namespace: NAMESPACE
      labels:
        project: PROJECT
    spec:
      containers:
         - name: PROJECT
           image: REGISTRY_HOSTNAME/PROJECT:TAG_VERSION
           ports:
             - containerPort: PORT_CONTAINER

           env:
             - name: S3_SECRET_ACCESS_KEY
               valueFrom:
                 secretKeyRef:
                   name: files-backend-production-config-secret
                   key: S3_SECRET_ACCESS_KEY
             - name: S3_REGION
               valueFrom:
                 secretKeyRef:
                   name: files-backend-production-config-secret
                   key: S3_REGION
             - name: S3_BUCKET_NAME
               valueFrom:
                 secretKeyRef:
                   name: files-backend-production-config-secret
                   key: S3_BUCKET_NAME
             - name: FILE_PORT
               valueFrom:
                 secretKeyRef:
                   name: files-backend-production-config-secret
                   key: FILE_PORT
             - name: S3_ACCESS_KEY_ID
               valueFrom:
                 secretKeyRef:
                   name: files-backend-production-config-secret
                   key: S3_ACCESS_KEY_ID
